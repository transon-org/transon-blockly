# Implement one requirement

Implement exactly one requirement by ID (the argument; if none, the first unchecked row for the active
milestone in `docs/traceability.md`). The procedure is tool-neutral and lives in
`harness/commands/implement-requirement.md` — follow it exactly.

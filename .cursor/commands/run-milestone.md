# Run a roadmap milestone

Implement the milestone given as the argument (e.g. `M2`), end to end. The procedure is tool-neutral
and lives in `harness/commands/run-milestone.md` — follow it exactly, under the rules in `AGENTS.md`.

# Session retrospective — M3: editor-blockly: G_palette/G_toolbox + Zelos + behavior runtime

> One-off harness retrospective. Evidence-based: only what happened in this session. Not a contract.
> Any field you cannot support from this session's history ⇒ write `not observed`.

- **Session date / model:** 2026-07-01 / Claude Opus 4.8 (`claude-opus-4-8`)
- **How the session started:** `/run-milestone M3`
- **Milestone worked on:** M3 — "editor-blockly: G_palette/G_toolbox + Zelos + behavior runtime" (name as it appeared in the ROADMAP tracker read during the session)

## Harness steps actually taken
- **Commands / skills invoked:** `/run-milestone M3` (kickoff); `AskUserQuestion` once (presentation-data values); `TaskCreate`/`TaskUpdate` for a 6-item tracker; git commits + gate scripts run via Bash.
- **Subagents used:** `milestone-planner` (produced the locked M3 design + 4 STOP items); `round-trip-reviewer` (adversarial review of the round-trip/safety-critical changes). Both actually spawned and returned.
- **Test-first observed?:** Partial — a per-requirement test citing the ID was written and run to green for each slice (e.g. `presentation.test.ts`, `toolbox.test.ts`, `palette.test.ts`, `palette-load.test.ts`, `blockly-load.test.ts`, `runtime.test.ts`, `ac036-`/`ac037-`, `decode.test.ts`); generally written alongside/after the implementation, not strictly failing-test-first. A pre-implementation headless Blockly experiment was run before writing the behavior runtime.
- **Gates run & result:** all passed as last observed — `check_traceability.py`, `check_links.py`, `check_engine_parity.py` (22 rules/28 ops/4 fns), `check_maturity.py --check` (93%, no regression), `run_evals.py`, `update_memory.py --check`, `check_no_codec_mapping.py`, `check_presentation.py` (+`--selftest`), `check_behavior_runtime_size.py` (+`--selftest`); byte-equal regen check via `regen.test.ts`. The pre-commit hook ran the gate suite on each commit. `review-gate` workflow: not run as a workflow (a `round-trip-reviewer` subagent was used instead).
- **Traceability / docs updated in the same change?:** Yes — `docs/traceability.md` rows updated within the slice commits; `docs/ROADMAP.md` (tracker ☐→☑) + `docs/current-state.md` updated in the final docs commit.
- **Memory / handoff refreshed?:** Yes — `update_memory.py --state` run (header regenerated) + `current-state.md` narrative updated. `--snapshot` not run (`--check` confirmed no drift).

## Friction, deviations, skips
- `check_behavior_runtime_size.py` FAILED first run (declared 4 primitives vs 3 counted — the field registration went through a `reg` alias the regex missed) → fixed by calling `fieldRegistry.register` directly → passed.
- `check_presentation.py` FAILED first real run (a category name in a doc comment false-positived) → fixed by stripping comments before the scan → passed.
- `encode.test.ts` array assertion FAILED after the encoder `extraState.items` change → expectation updated → passed.
- Headless Blockly import experiment errored twice (`blockly/core.js` not an exports subpath; then `Blockly.*` undefined under raw-node ESM) → resolved (default export under raw node; Vite-interop namespace under vitest).
- Palette/toolbox artifact bootstrap required a "double-build" (placeholder JSON → `UPDATE_ARTIFACTS` → rebuild → `UPDATE_ARTIFACTS` again); pass 1 of the palette regen check failed byte-equal as expected, pass 2 passed.

## Human interventions
- Answered the `AskUserQuestion` on presentation-data values → chose "Author with defaults (Recommended)."
- Mid-session (after milestone completion) asked whether there is validation with real Blockly plugged in → I listed the real-Blockly test files.

## Outcome
- **What landed:** 7 commits on branch `m3-editor-blockly` (`12b2751` D0, `e17b28f` D1, `16d59ca` D2, `3b2e515` D3a, `cd18877` D3b, `f4de4c8` review fix, `a030cf1` docs). New `@transon/editor-blockly` package; `presentation.json` + `G_palette`/`G_toolbox` projections + committed `palette.json`/`toolbox.json`; behavior runtime (1 field + 3 mutators); new gates `check_presentation.py` + `check_behavior_runtime_size.py` (wired into pre-commit + CI); multiple new test files. Last observed test totals: 1387 passing (1364 adapter + 12 core + 11 blockly).
- **Milestone/DoD status as stated in the session:** M3 marked done (ROADMAP tracker ☑); `round-trip-reviewer` verdict "READY" after its one SHOULD-FIX (empty-array reverse-path decode crash) was fixed and promoted to a reverse-path gate; all DoD gates green; branches not pushed.

## Not observed / unverifiable
- Session wall-clock duration — not observed.
- Any branch push / PR — not done (explicitly left for later).
- Interactive/visual Blockly rendering (Zelos renderer, field widgets, drag/drop) — not exercised; all Blockly validation was headless.
- After the final commit, working-tree files (`blocks.ts`, `index.ts`, `palette.test.ts`) were modified outside this session's flow (adding `ruleTooltip`/FR-078/FR-129 content) — those edits were not made by this session and their history is unverifiable here.

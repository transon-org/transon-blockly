# Session retrospective — M0: engine `switch`/`cond` + projection-ready export + Node adapter (editor-side)

> One-off harness retrospective. Evidence-based: only what happened in this session. Not a contract.
> Any field you cannot support from this session's history ⇒ write `not observed`.

- **Session date / model:** 2026-07-01 (stated at session start); Opus 4.8 (`claude-opus-4-8`).
- **How the session started:** A free-text prompt asking how to start implementation, what the routine to implement `docs/ROADMAP.md` is, and which commands/prompts to use. M0 work was then driven by `/run-milestone M0`.
- **Milestone worked on:** M0 — engine `switch`/`cond` + projection-ready export + Node adapter. Editor-side scope actually done this session: pnpm/Turborepo monorepo scaffolding + `@transon/editor-core` stub + the Node→Python `test/engine-node-adapter`.

## Harness steps actually taken
- **Commands / skills invoked:** `/run-milestone M0`; chapter marker "M0 milestone implementation" set via `mark_chapter`. No `Skill` tool invocations observed.
- **Subagents used:** `milestone-planner` (M0 editor-side plan); `general-purpose` (M0 build — monorepo scaffolding, `editor-core` port + snapshot loader, Node→Python adapter, `@`/`$` marker test); `round-trip-reviewer` (M0 review of the marker-staging / adapter slice).
- **Test-first observed?:** Partial — the build was instructed to work test-first; final test names cite requirement IDs (AD-008, AD-011, and FR-116/FR-119/AD-027/AD-030 on the marker test). A failing-test-before-implementation step was not directly observed (subagent ran autonomously; only the final green suite was seen).
- **Gates run & result:** `check_engine_parity.py` (consistent — 22 rules / 28 operator tokens / 3 functions); `check_traceability.py` (consistent); `check_links.py` (resolve); `update_memory.py --check` (OK); `check_maturity.py --check` (no regression); `run_evals.py` (passed, in pre-commit output); `pnpm typecheck` (3/3), `pnpm -r test` (13/13 — 6 editor-core + 7 adapter), `pnpm -r build` (clean, ESM + `.d.ts`). `review-gate` workflow: not run — `round-trip-reviewer` subagent used instead.
- **Traceability / docs updated in the same change?:** Yes — `docs/traceability.md` rows set to `[~]` with test refs, and `docs/ROADMAP.md` M0 tracker/status, included in the M0 commit `8751707`.
- **Memory / handoff refreshed?:** Yes — `update_memory.py --state` run (header regenerated); `docs/current-state.md` narrative updated; handoff-refresh commit `eb1fe81`.

## Friction, deviations, skips
- Environment probe: `transon` was not importable via bare `python3` (`ModuleNotFoundError`) but was importable via `../transon/.venv/bin/python`; resolved to use the venv (`TRANSON_PYTHON`).
- Engine pin: local `../transon/.venv` was at v0.1.2, ahead of the snapshot-pinned commit `5812b63`; `check_engine_parity.py` ran green against it, so no checkout/re-pin was done.
- Node adapter: `transon.__version__` was not set, so the runner derived engine version from `get_editor_metadata()["engine_version"]` (`"0.1.1"`).
- `round-trip-reviewer` findings: a hardcoded `DEFAULT_VENV_PYTHON` absolute path (fixed → venv-relative resolution, verified by the suite passing with `TRANSON_PYTHON` unset) and a stale doc comment in `snapshot.ts` (fixed). The reviewer also flagged the uncommitted working tree as a "merge blocker" — that was the intended pre-commit state.

## Human interventions
- Early Q&A on the implementation routine + which commands to use.
- A clarification question — "what is the difference between options 1 and 2?" — about how to kick off the first M0 pass.
- Chose "Commit on the branch" for M0.

## Outcome
- **What landed:** Commit `8751707` ("editor: M0 scaffolding + Node→Python engine adapter") + `eb1fe81` ("docs: refresh M0 handoff post-commit (8751707)"), on branch `m0-editor-scaffolding`. Files: root monorepo configs (`package.json`, `pnpm-workspace.yaml`, `turbo.json`, `tsconfig.base.json`, `tsconfig.json`, `.changeset/`, `.npmrc`, `vitest.workspace.ts`, `pnpm-lock.yaml`), `packages/editor-core/**` (`engine/ports.ts`, `metadata/snapshot.ts`, `index.ts`, `vite.config.ts`, tests), `test/engine-node-adapter/**` (`index.ts`, `runner.py`, tests), and `docs/ROADMAP.md` / `docs/traceability.md` / `docs/current-state.md`.
- **Milestone/DoD status as stated in the session:** ROADMAP M0 tracker flipped to ☑; DoD gates all pass (13 tests, build, parity, traceability); `round-trip-reviewer` verified the trust-critical items with counterfactual tests and its one must-fix was applied.

## Not observed / unverifiable
- Correctness of the engine-side M0 (`switch`/`cond` + the `get_editor_metadata()` export): these pre-dated this session; only the venv's export was probed (`metadata_version 2.0`, 22 rules). The engine's code/tests were not reviewed or run this session.
- The CI engine-pin flip (M-09 / `--require-engine`) — noted deferred, not done this session.
- Whether the `m0-editor-scaffolding` branch was pushed or a PR opened — not done this session.
- Exact test-first ordering inside the build subagent (it ran autonomously).
